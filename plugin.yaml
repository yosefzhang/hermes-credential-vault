name: hermes-credential-vault
version: 0.1.0
description: Per-user encrypted credential vault for enterprise system tokens (JIRA / Confluence / PMS). Credentials never enter agent memory, session store, or LLM context.
author: Yosef & Yosephine
provides_tools:
  - call_external_system
provides_hooks:
  - pre_gateway_dispatch
  - transform_tool_result
